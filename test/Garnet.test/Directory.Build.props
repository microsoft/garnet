<Project>
  <!-- Import the global Directory.Build.props -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
  
  <!-- Define compilation symbol for test project -->
  <PropertyGroup>
    <DefineConstants>$(DefineConstants);TEST_PROJECT</DefineConstants>
  </PropertyGroup>

  <!-- Include shared global usings file for server projects -->
  <ItemGroup>
    <Compile Include="$(MSBuildThisFileDirectory)../../libs/GlobalUsings.cs" Link="GlobalUsings.cs" />
  </ItemGroup>
</Project>